fun main() {
//    val numbers = listOf("one", "two", "three", "four").filter { it.length > 3 }
//    numbers.forEach {
//        println(it)
//    }
    val obj = Demo("Data")
    println(obj.name)
}